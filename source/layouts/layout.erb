<!doctype html>
<html>
	<head>
		<meta charset="utf-8">
		<meta content="IE=edge,chrome=1" http-equiv="X-UA-Compatible">
		<title><%= current_page.data.title || 'Vidéos Ubuntu Party' %></title>
		<%= stylesheet_link_tag %w(all) %>
	</head>
	<body>
		<%= partial 'headers' %>
		<div id="content">
			<%= yield %>
		</div>
	</body>
</html>
